<ul class="tags_column">
    {{#foreach tags}}
    <a href="{{url}}" class="tags_column"><i class="fa fa-tags excerpt_tags" aria-hidden="true"></i>{{name}}<span class="post-count">{{post_count}}</span></a>
    {{/foreach}}
</ul>
